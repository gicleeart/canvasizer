name: Hello Action

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "✅ Hello, Wesley! Your Actions are working."
